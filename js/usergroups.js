jQuery(document).ready(function () {	//console.log('woo');	//userSearch.init('ef-post_following_users_search' , 'ef-post_following_users');	//userSearch.do_search();	jQuery('ul#ef-post_following_users li').quicksearch({		position: 'before',		attached: 'ul#ef-post_following_users',		loaderText: '',		delay: 100	})	});/** * */ userSearch = {		var $search_box, $user_list,		/**	 *	 */	init : function(search_box_id, user_list_id) {		$search_box = jQuery('#'+search_box_id);		$user_list = jQuery('#'+user_list_id);				$search_box.keyup(this.do_search);		},	/**	 *	 */	do_search : function(e, data) {		$searchBox = jQuery(e.eventTarget);				search_string = $searchBox.val(); // need to trim this		search_string = 'admin';		if(search_string.length > 0) {			// SHOW X button			$searchBox.children('').show()						jQuery('<span></span>')				.text('x')				.addClass('ef-search_clear') // USE SAME CLASS AS TAG x				.appendTo($searchBox)				.click(clear_search);				;						var $unmatched_users = $search_box.children('*:not(:contains("'+ search_string +'"))').hide();			var 		}			},		clear_search : function( ) {		$user_list.children().show();	}}